# Backend configuration
# MONGODB_URI is required. Backend startup fails without MongoDB.

PORT=5000
JWT_SECRET=alumlink_secret
SEED_DEMO=false

# MongoDB (required)
MONGODB_URI=mongodb://127.0.0.1:27017/alumlink
