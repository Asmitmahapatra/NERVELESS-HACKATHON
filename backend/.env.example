# Backend configuration
# If you don't set MONGODB_URI, the backend runs in DEMO MODE (in-memory data).

PORT=5000
JWT_SECRET=alumlink_secret
SEED_DEMO=true

# MongoDB (optional)
# MONGODB_URI=mongodb://127.0.0.1:27017/alumlink
