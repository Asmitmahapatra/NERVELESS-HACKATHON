# Backend configuration
# This file is safe to commit: do not put real secrets here.

PORT=5000
NODE_ENV=development

# Use a strong secret in production via environment variables or a local, untracked file.
JWT_SECRET=change-me

# MongoDB (required). For local dev, use local MongoDB.
MONGODB_URI=mongodb://127.0.0.1:27017/alumlink

# Demo seed (optional)
SEED_DEMO=false
